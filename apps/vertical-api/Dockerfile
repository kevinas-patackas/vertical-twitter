# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build vertical-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t vertical-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system vertical-api && \
          adduser --system -G vertical-api vertical-api

COPY dist/apps/vertical-api vertical-api
RUN chown -R vertical-api:vertical-api .

CMD [ "node", "vertical-api" ]
